import 'package:flutter/material.dart';

class CattleScreen extends StatelessWidget {
  const CattleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Manage your cattle here'),
    );
  }
}
